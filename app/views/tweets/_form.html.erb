<%= form_for Tweet.new do |f| %>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, rows: 5, class: "form-control" %>
  </div>
  <!--
  <div class="field">
    <%#= f.label :picture %><br>
    <%#= f.select(:picture, Picture.all.each{|p|  p.image_url(:thumb)}) %>
  </div> -->
  <div class="actions">
    <%= button_to "Отправить", @tweet, method: :create, remote: true, class: "btn btn-primary" %>
  </div>
<% end %>